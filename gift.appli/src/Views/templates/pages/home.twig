{# src/Views/templates/pages/home.twig #}
{% extends "layout.twig" %}

{% block title %}Bienvenue sur MyGiftBox - Créez des coffrets cadeaux personnalisés{% endblock %}

{% block content %}
    <div class="relative bg-indigo-800 mb-8">
        <div class="container mx-auto px-4 py-16 md:py-24">
            <div class="max-w-3xl text-white">
                <h1 class="text-4xl md:text-5xl font-bold mb-4">Offrez l'expérience parfaite avec MyGiftBox</h1>
                <p class="text-xl md:text-2xl mb-8">Créez des coffrets cadeaux uniques et sur mesure pour toutes les occasions spéciales.</p>
                <div class="flex flex-wrap gap-4">
                    <a href="{{ base_url() }}/categories" class="px-6 py-3 bg-white text-indigo-800 rounded-lg font-semibold hover:bg-indigo-100 transition-colors">
                        Explorer le catalogue
                    </a>
                    <a href="{{ base_url() }}/register" class="px-6 py-3 bg-transparent border-2 border-white text-white rounded-lg font-semibold hover:bg-white hover:text-indigo-800 transition-colors">
                        Créer un compte
                    </a>
                </div>
            </div>
        </div>
        <div class="absolute bottom-0 right-0 w-1/3 h-full bg-right bg-no-repeat hidden lg:block" style="background-image: url('{{ base_url() }}/img/hero-image.jpg');"></div>
    </div>

    <div class="container mx-auto px-4 py-12">
        <!-- Comment ça fonctionne -->
        <section class="mb-20 h-[60vh] flex flex-col justify-center items-center">
            <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Comment ça fonctionne</h2>
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="bg-white p-6 rounded-lg shadow-md text-center">
                    <div class="w-16 h-16 bg-indigo-100 text-indigo-800 rounded-full flex items-center justify-center text-2xl font-bold mx-auto mb-4">1</div>
                    <h3 class="text-xl font-semibold mb-3">Explorez notre catalogue</h3>
                    <p class="text-gray-600">Parcourez notre sélection de prestations exceptionnelles classées par catégories.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md text-center">
                    <div class="w-16 h-16 bg-indigo-100 text-indigo-800 rounded-full flex items-center justify-center text-2xl font-bold mx-auto mb-4">2</div>
                    <h3 class="text-xl font-semibold mb-3">Composez votre coffret</h3>
                    <p class="text-gray-600">Créez un coffret entièrement personnalisé ou partez d'un modèle prédéfini.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md text-center">
                    <div class="w-16 h-16 bg-indigo-100 text-indigo-800 rounded-full flex items-center justify-center text-2xl font-bold mx-auto mb-4">3</div>
                    <h3 class="text-xl font-semibold mb-3">Offrez l'expérience</h3>
                    <p class="text-gray-600">Partagez l'URL unique du coffret avec le destinataire de votre choix.</p>
                </div>
            </div>
        </section>

        <!-- Catégories populaires -->
        <section class="mb-20 h-[40vh]">
            <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Découvrez nos catégories</h2>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                {% for categorie in categories %}
                    <a href="{{ base_url() }}/categorie/{{ categorie.id }}" class="group">
                        <div class="bg-white rounded-lg shadow-md overflow-hidden h-64 relative">
                            <div class="absolute inset-0 bg-cover bg-center group-hover:scale-105 transition-transform duration-300" style="background-image: url('{{ base_url() }}/img/{{ categorie.libelle|lower }}.jpg');"></div>
                            <div class="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"></div>
                            <div class="absolute bottom-0 left-0 p-6 text-white">
                                <h3 class="text-xl font-bold mb-1">{{ categorie.libelle }}</h3>
                                <p class="text-sm line-clamp-2">{{ categorie.description }}</p>
                            </div>
                        </div>
                    </a>
                {% endfor %}
            </div>

            <div class="text-center mt-8">
                <a href="{{ base_url() }}/categories" class="inline-block px-6 py-3 bg-indigo-600 text-white rounded-lg font-semibold hover:bg-indigo-700 transition-colors">
                    Voir toutes les catégories
                </a>
            </div>
        </section>

        <!-- Avantages -->
        <section class="mb-20 h-[60vh] flex flex-col justify-center items-center">
            <h2 class="text-3xl font-bold text-center mb-12 text-gray-800">Pourquoi choisir MyGiftBox ?</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h3 class="text-xl font-semibold mb-3 text-indigo-600">Personnalisation complète</h3>
                    <p class="text-gray-600">Contrairement aux coffrets traditionnels, MyGiftBox vous permet de composer librement votre coffret à partir d'un vaste catalogue de prestations. Adaptez-le parfaitement aux goûts du destinataire.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h3 class="text-xl font-semibold mb-3 text-indigo-600">Flexibilité totale</h3>
                    <p class="text-gray-600">Partez d'une box vide ou basez-vous sur nos coffrets types prédéfinis que vous pourrez ensuite adapter selon vos envies : modifier les quantités, ajouter ou retirer des prestations.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h3 class="text-xl font-semibold mb-3 text-indigo-600">Partage simplifié</h3>
                    <p class="text-gray-600">Une fois votre coffret validé, partagez simplement l'URL unique avec le destinataire de votre choix, par email, SMS ou messagerie. Ajoutez un message personnalisé pour rendre votre cadeau encore plus spécial.</p>
                </div>
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h3 class="text-xl font-semibold mb-3 text-indigo-600">Expériences de qualité</h3>
                    <p class="text-gray-600">Nous sélectionnons avec soin chaque prestation de notre catalogue pour vous garantir des expériences exceptionnelles, des moments inoubliables et des souvenirs précieux.</p>
                </div>
            </div>
        </section>

        <!-- Call to action -->
        <section class="h-[60vh] flex flex-col justify-center items-center">
            <div class="bg-indigo-100 rounded-lg p-8 md:p-12  w-full h-90 flex flex-col justify-center items-center">
                <h2 class="text-2xl md:text-3xl font-bold mb-4 text-indigo-800">Prêt à créer votre coffret personnalisé ?</h2>
                <p class="text-lg mb-6 text-indigo-700">Commencez dès maintenant et offrez une expérience unique.</p>
                <div class="flex flex-wrap justify-center gap-4">
                    <a href="{{ base_url() }}/categories" class="px-6 py-3 bg-indigo-600 text-white rounded-lg font-semibold hover:bg-indigo-700 transition-colors">
                        Explorer le catalogue
                    </a>
                    <a href="{{ base_url() }}/register" class="px-6 py-3 bg-white text-indigo-800 border border-indigo-600 rounded-lg font-semibold hover:bg-indigo-50 transition-colors">
                        Créer un compte
                    </a>
                </div>
            </div>
        </section>
    </div>
{% endblock %}