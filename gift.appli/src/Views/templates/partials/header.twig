<div class="container mx-auto px-4 py-8 flex justify-between items-center">
    <div>
        <a href="{{ base_url() }}" class="text-2xl font-bold">Gift App</a>
    </div>

    <nav>
        <ul class="flex space-x-6">
            <li><a href="{{ base_url() }}" class="hover:text-gray-300">Accueil</a></li>
            <li><a href="{{ base_url() }}/categories" class="hover:text-gray-300">Catégories</a></li>

            {% if user %}
                <li>
                    <a href="{{ base_url() }}/logout" class="hover:text-gray-300">
                        Déconnexion
                    </a>
                </li>
            {% else %}
                <li>
                    <a href="{{ base_url() }}/auth" class="hover:text-gray-300">
                        Connexion / Inscription
                    </a>
                </li>
            {% endif %}
        </ul>
    </nav>
</div>